# Log

## Timeline

- Created task
- Selected `docs/architecture.md` as the primary anchor for the core model and `docs/self-evolution-mcp-plan.md` as the evolution-specific boundary note.
- Added architecture guidance for the hard-state versus soft-cognition split, artifact and task role separation, reconstructability, composite artifacts, verification layering, promotion, invalidation, and the authority boundary for external intelligence.
- Recorded the recommended next step as defining a `FeatureChangeArtifact` protocol rather than attempting a universal composition engine first.

## Notes

- This task changes documentation only. It does not claim that the full artifact graph runtime has already been implemented.
- The new text is meant to clarify the target model while preserving the existing layered implementation diagram.

## Follow-ups

- Define `FeatureChangeArtifact` with slots, invariants, verification obligations, promotion gate, invalidation matrix, and reconstruction envelope fields.
